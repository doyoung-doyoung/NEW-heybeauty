# Hey! Beauty — 작업 메모리

새 세션은 이 파일을 자동으로 읽는다. 별도 붙여넣기 없이 여기서 맥락을 이어받는다.
**마지막 갱신: 2026-09-20**

## 0. 지금 바로 할 일

- [ ] **도도가 첨부한 이미지를 받아 `public/` 아래에 넣기.** 규격·경로·파일명은 `IMAGE_TODO.md`에 전부 적혀 있다.
  처리 순서: ① 규격대로 리사이즈 + **중앙 기준** 크롭 ② 500KB 이하로 압축 ③ 정확한 파일명으로 저장
  ④ `shasum public/clinics/*.jpg | awk '{print $1}' | sort -u | wc -l` 로 서로 다른 파일인지 확인 ⑤ 커밋·푸시
- [ ] 슬립·ID카드·제품박스·화면캡처 이미지가 들어오면 **코드 연결**:
  `components/home/DemoAssets.tsx`의 `SlipImage` · `IdCardImage` · `BoxLabelImage`를 CSS 드로잉에서 `<img>`로 교체,
  `components/partner/AiInput.tsx`의 `SCREEN_CAPTURES`에 이미지 추가
- [ ] `og.jpg`가 들어오면 `app/layout.tsx`의 `metadata`에 openGraph 이미지 추가 (현재 설정 자체가 없어 링크 미리보기가 빈 상태)

### 그다음 (아직 미착수)

- [ ] **A. 번역** — `lib/i18n.ts`를 쓰는 건 홈 탭 2개 파일뿐. 채팅·예약·후기·어드민·파트너·회사소개와 시드 데이터가 전부 한국어라 TH를 눌러도 껍데기만 바뀐다.
- [ ] **B. 후기코드 커미션** — 코드를 넣는 화면 자체가 없고 정산이 ฿150에서 안 움직인다.

(도도가 고른 A·B·C 중 **C = 홈 예약 → 파트너 CRM 고객 연결은 완료**했다. §2 참조)

## 1. 프로젝트

태국 뷰티 클리닉 **O2O 플랫폼 + 클리닉 AI CRM/재고관리**를 보여주는 **투자자·파트너 시연용 MVP**.
한 페이지에 상단 탭 4개(홈 / 어드민 / 파트너 CRM / 회사소개). Next.js 15 + Tailwind.
상세 스펙은 `HEYBEAUTY_SPEC.md`, 남은 이미지는 `IMAGE_TODO.md`.

데이터는 서버 없이 `lib/seed.ts`가 만든 시드를 localStorage(`heybeauty.db.v1`)에 넣고 쓴다.
`lib/storage/index.ts`의 한 줄만 바꾸면 Supabase로 갈아끼울 수 있게 어댑터로 분리돼 있다.

**`lib/seed.ts`의 데이터 내용을 고치면 `SEED_VERSION`을 반드시 함께 올린다.**
안 올리면 이미 사이트를 열어본 사람에게 옛 데이터가 남아 바뀐 게 안 보인다. (현재 **5**)

파트너 CRM 데모 로그인: `master1` / `hb1000` (아무 값이나 통과)

## 2. 최근 작업 — 홈 예약 → 파트너 CRM 고객 연결 (완료)

예약만 만들어지고 클리닉 CRM에는 고객 카드가 안 생기던 문제를 고쳤다.

- **`lib/crm.ts`** → `linkBookingToCustomer(db, booking)`: 같은 지점에 `appUserId` 또는 `phone`이 같은 카드가 있으면
  **재사용**하고 관심 시술만 추가, 없으면 유입경로 `App`으로 새로 만든다. `booking.customerId`까지 채우고 고객을 반환한다.
  예약 생성 코드는 전부 이 함수를 거친다 (시드 포함).
- 타입: `Gender`에 `"미확인"` 추가 / `Customer.appUserId` / `Booking.customerId`
- 앱 예약 직후 고객의 생년월일·성별·국적은 **"미확인"으로 둔다.** 방문 전이라 실제로 모르는 값이고,
  스펙상 방문 때 ID카드 OCR로 채우는 흐름이다. 임의값으로 채우면 데모 CRM 통계가 가짜가 된다.
- 화면: 고객 상세에 "예약 내역" 카드 + 앱 계정 행 / 예약 확인에 연결 고객명 + `고객 카드 열기` +
  카드 없는 예약용 `고객 등록` 버튼 / 성별 필터에 "미확인"

## 3. 작업 규칙

- **브랜치 `claude/vigilant-mendel-43gwy1`에서 작업하고 여기에만 푸시한다.** PR은 도도가 명시적으로 요청할 때만.
- 커밋 전 **`npm run build`와 eslint를 통과시킨다.** 화면을 건드렸으면 **375×812에서 `document.scrollWidth === 375`**도 확인한다(가로 스크롤 금지는 이 프로젝트의 오래된 원칙).
- 주석은 한국어로, 주변 코드 스타일에 맞춘다. 왜 이렇게 했는지가 안 보이는 곳에만 단다.
- 커밋 메시지는 한국어. 무엇을 왜 고쳤는지 쓴다.

### 함정 (실제로 당한 것들)

- **`npm install` 하면 `package-lock.json`이 바뀐다.** 커밋 전에 `git checkout package-lock.json`으로 되돌릴 것.
- **`IMAGE_TODO.md`의 `sips` 명령은 맥 전용.** 원격 리눅스 세션에서 이미지를 변환할 땐 ImageMagick이나 Python PIL을 쓴다.
- **`.next` 캐시가 어긋나면** 정적 청크가 400을 뱉고 화면이 "데모 데이터를 불러오는 중..."에서 멈춘다. 서버를 완전히 죽이고 다시 띄우면 된다.
- 시드 배열 인덱스가 한 칸 밀려 **클리닉과 사진이 어긋난 적이 있다.** 이미지를 넣은 뒤엔 `shasum`으로 장수만큼 서로 다른지 확인한다.

## 4. 말투·표현 규칙

- **한국어로 대화한다.** 사용자 호칭은 "도도".
- 과장·사족 없이 직설적으로. 완료 보고에는 검증 결과를 함께 적는다. 안 한 것은 안 했다고 말한다.
- **어드민·파트너 CRM·회사소개는 한국어 고정**(언어 버튼 없음). 다국어는 홈 탭만.
- 통합 인박스에서 자사 앱 채널은 화면에 **"헤이뷰티"**로 보이되 **저장 데이터 값은 `"App"` 그대로 둔다.**

## 5. 참고 링크

- 이미지 제작 브리프(아티팩트, 비공개): https://claude.ai/artifact/STFrqdkTG6T9kRHiJakUUY
- 저장소: `doyoung-doyoung/NEW-heybeauty`
