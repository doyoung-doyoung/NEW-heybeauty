# Hey! Beauty MVP — 개발 스펙 (Claude Code 인계용)

> 작성일: 2026-09-17 | 출처: claude.ai 프로젝트 "mvp 헤이뷰티" 대화
> 이 파일을 프로젝트 루트에 두고 Claude Code에게 "HEYBEAUTY_SPEC.md 읽고 Phase 1부터 시작해"라고 요청하세요.

---

## 0. 작업 규칙 (사용자 선호)

- 사용자: 도도(RAON (Thailand) Co., Ltd. 대표), 코딩 초보. 설명은 **한국어 반말·친근한 톤**, 이모지 OK
- 항상 **어떤 파일을 수정하는지**, **어떤 터미널에서 실행하는지** 명시
- 단계는 **번호 목록**, 개념은 **쉬운 비유**로 설명
- **JSX/코드 안에는 이모지 금지**
- 조금이라도 불확실하면 추측하지 말고 **사용자에게 질문**
- 평소 스택: Next.js 15, GitHub, Vercel (+ Supabase)
- 참고: 사용자 맥에 `rtk`(토큰 절약, Claude Code에 자동 적용), `archify`(아키텍처 다이어그램 스킬)가 설치되어 있음. 아키텍처 다이어그램은 archify로 다시 그려줄 것

---

## 1. 서비스 개요

- **서비스명:** Hey! Beauty / **회사:** RAON (Thailand)
- **정체:** 태국 뷰티 클리닉 **O2O 플랫폼(소비자 AI 채팅 → 추천 → 예약·결제)** + **클리닉용 AI CRM/재고관리**를 보여주는 **투자자·파트너 시연용 MVP**
- 비유: 소비자 쪽은 "배민", 클리닉 쪽은 "포스기", 어드민은 "배민 본사"
- **모든 데이터는 가라(mock)**. 단, 가라 데이터는 **전부 클릭해서 상세 확인 가능**, **수정 → 저장 → 새로고침 후에도 유지**
- **탭 간 연동은 반드시 같은 데이터 소스** 사용 (재고 차감 → 어드민 숫자 동일 반영 등)
- 시연 환경: **PC + 폰 둘 다** (반응형 필수)

---

## 2. 확정 결정사항

| # | 결정 | 비고 |
|---|---|---|
| 1 | 한 페이지 + **상단 탭 4개**(홈 / 어드민 / 파트너 CRM / 회사소개) | MVP라 한 화면에서 전부 시연 |
| 2 | 데이터 저장: **최종 Supabase** | 폰·PC 어디서든 유지, Realtime으로 탭 간 동기화 |
| 3 | 배포: **먼저 가볍게 → 나중에 Vercel** | claude.ai 아티팩트 링크로 먼저 하려 했으나 Claude Code로 이동. Claude Code에서는 아티팩트 게시 불가 → **Phase 1: 로컬(localStorage 어댑터)** → **Phase 2: Supabase + Vercel** 로 대체 (사용자에게 스택/순서 최종 확인할 것) |
| 4 | 저장 로직은 **storage 어댑터 한 곳에 모으기** | localStorage ↔ Supabase 교체만으로 이사 가능하게 |
| 5 | 가라 데이터는 **seed JSON 파일 하나로 분리** | 나중에 Supabase 초기 데이터로 그대로 사용 |
| 6 | **데모 리셋 버튼** ("초기 데이터로 되돌리기") | 시연 반복 대비 |
| 7 | 언어: **홈 탭에만** 언어 버튼. EN·TH 전체 번역 / ZH·RU **항목명만** / **AR은 탭만 보이고 클릭 시 Coming soon** | 어드민·파트너·회사소개는 언어 버튼 없음(기본 영어 or 한국어 — 사용자 확인) |
| 8 | 클리닉 채팅은 **자동 답변** | 파트너 탭에서 직접 답장 기능은 불필요 |
| 9 | 로그인 **LINE(가라)**, 파트너 로그인은 **마스터/지점 버튼 2개**, ID·PW 아무거나 입력하면 통과, 둘 다 같은 CRM 화면으로 이동 | |
| 10 | 재고 경고: **제품별 재고율(%) 설정** 기능 포함 + 기본 기준 **50개 이하 빨간색** | 빨간 항목 클릭 → 구매하기(RAON에 주문) |
| 11 | 재고 차감 시점: **시술 후 음성/사진으로 사용 제품 입력 시** | |
| 12 | 예약: **날짜·시간·의사 선택 + 예약금 1,000฿ 선결제 + 취소규정은 문구만** | |
| 13 | 유저 **"내 예약 / 후기 쓰기"** 는 AI 채팅 **왼쪽 탭 안**에 배치 | |
| 14 | 예약유도 질문은 **1회차부터 클릭 가능** (2회차에도 나옴) | |
| 15 | 재고 유통 형태 표기: **"정식 / 병행수입"** ("그레이" 표현 사용 안 함) | 태국 FDA 민감 이슈 |
| 16 | 후기코드 흐름: 방문 완료 → 어드민 코드 발행 → 유저가 코드로 후기 작성 → 어드민 후기 승인 → **다른 유저가 그 코드로 예약·결제 → 코드 주인에게 커미션** | |
| 17 | 어드민 **차단** 대상: **유저, 후기** / **비번 관리**: 설정된 모든 비번(클리닉 계정 + 유저) | |
| 18 | ID카드·송금슬립 예시 이미지: **가상 은행명(예: HeyBank)·가상 인물·가상 번호 + "DEMO" 워터마크**로 제작. 실제 태국 신분증/실제 은행 슬립과 동일한 이미지는 만들지 않음 | 슬립 사기 악용 위험 |
| 19 | Coming soon 처리: **직원별 인센티브, 영상 상담/라이브, B&A 사진 분석, 사주/관상, 위치기반 100m 알림** | 메뉴는 보이고 클릭 시 Coming soon |
| 20 | 이미지 **"추가하기" 칸** 제공(어드민·파트너 CRM 제품 등록) | 사용자가 계속 업데이트 예정 |

---

## 3. 화면 구성 상세

### ① 홈 탭 (소비자) — 상단 언어 버튼
**A. AI 채팅 (ChatGPT 스타일)**
- 왼쪽 사이드바: 이전 대화 리스트(가라), **"클리닉 A와 대화 · 예약 관련"** 같은 클리닉 대화 기록, **내 예약 / 후기 쓰기**
- 첫 화면에 **첫 질문 2개** 버튼 (아래 §5 시나리오)
- 답변마다: **답변 + 예상질문 2개 + 예약유도 질문 1개** (모두 클릭 연동)
- 예약유도 클릭 → 해당 시술 클리닉 카드 → 예약(날짜·시간·의사) → **PromptPay QR 생성** → 채팅에서 **이미지 첨부 클릭 시 저장된 슬립 이미지 목록** → 선택 → **송금 확인 완료** → **"클리닉과 채팅하기"** 버튼
- 클리닉 채팅: 자동 답변. 대화 종료 시 AI 채팅 왼쪽 리스트에 기록 남음 + **파트너 CRM 인박스에도 표시**

**B. 일반 페이지 (클리닉 목록/상세)**
- 등록된 모든 클리닉: 이름, 가격, 프로모션, 위치, 후기(후기코드 포함), 시술목록, 의사, 영업시간, 주차 등
- 카드 클릭 → 상세 → 예약 가능

### ② 어드민 탭 (RAON)
- 클리닉 정보 조회/수정/저장
- 비번 관리(클리닉 계정 + 유저)
- 차단(유저, 후기)
- 공지 등록, 팝업 등록(홈에 실제로 뜨게)
- 후기 승인 / 후기코드 발행 / 커미션 내역
- **전체 재고 현황**: 파트너 재고와 동일 숫자, 차감 즉시 반영, **빨간 경고 항목 표시** → 구매하기
- 데모 리셋 버튼

### ③ 파트너 CRM + 인벤토리 탭
- 로그인: **[마스터] [지점]** 버튼 → ID/PW 아무거나 → 동일 CRM 화면 (지점 있는 클리닉은 지점 선택)
- **AI 입력 채팅창**(홈 채팅과 같은 레이아웃) + **왼쪽 탭에 이전 입력 기록 리스트(가라)**
  - **카메라 버튼:** "사진 찍기" + **등록해둔 예시 이미지 목록**(제품 사진, 제품 박스, ID카드, 캡처 화면) → 클릭 시 **텍스트로 추출된 메시지** 표시 → **수정 가능** → 저장 → "저장 완료되었습니다"
  - **마이크 버튼:** "말하기" → **3초간 듣는 애니메이션** → 인식된 문장 표시 → 수정 → 업데이트/저장 → "저장되었습니다"
  - 박스 사진 → 박스 문구(제품명·용량·Lot·유통기한) 읽어 입력 → 수정 → 저장
- **통합 인박스**: LINE · Meta(FB/IG) · Hey! Beauty 앱 채팅 문의(가라, 실제 상담처럼)
- 고객 관리 / 예약 확인 / 전자차트(OPD)
- 재고 / 통계 / SMS / AI 프로모션 제안 (§4)

### ④ 회사 소개 탭
- Hey! Beauty / RAON (Thailand) 소개. 이 스펙의 서비스 개요·기능을 바탕으로 작성 (투자자 대상 톤)

---

## 4. CRM 기능 요구사항 (원문 스펙 반영)

전제: CRM의 기본 기능은 일반 CRM과 동일

1. **AI 에이전트 정보입력**
   - 병원 기본정보(병원명, 시술목록, 가격, 의사, 영업일/시간, 위치, 주차) — 음성/이미지/채팅
   - 이미지(캡처)로 정보 이관: 페이스북, 고와비, 사용중인 CRM
   - **방문 전:** 채팅(메신저·앱 메신저)에서 정보 추출 → 고객정보 자동입력(경로/관심시술/예약방문일/의사)
   - **입장:** ID카드 이미지 업로드 → 개인정보 자동입력
   - **방문 후:** 음성으로 시술·결제내용·의사 소견, 담당 의사/간호사/직원, 사용 제품 항목/양/공급가(할인 반영), 사용 소모품 → **재고 차감**
   - 입력 내용 **요약 → 확인/수정 → 담당자 이름(사원번호) 입력 → 확인**
2. **통계 (가라 차트)**: 기간별(병원 성과) / 환자별(상담·방문·치료) / 재고별(입고·사용) / 경로별(채널 유입) / 직원별(인센티브 → **Coming soon**)
3. **SMS 전송(가라)**: 기념일(생일/첫 방문), 시술 주기 재방문 유도, 프로모션 안내
4. **AI 프로모션 자동 제안**: 구매 패턴 기반(예: 쏭크란 이후 증가 시술) → 프로모션 제안 + 프로모션 이미지 생성(가라)
5. **AI 재고관리**
   - 제품명 음성 입력 + 제품 이미지 자동 매칭 → 확인
   - 유통 형태: **정식 / 병행수입**
   - 제품 정보: 용량, 유통기한, 구매처, 담당자, 구매날짜, 구매가격, 판매가격, Lot번호
   - **설정 재고율(%) 도달 시 구매 알림** + 기본 50개 이하 빨간색
   - 유통사 프로모션 진행 시 구매 유도 알림
6. **24시간 챗봇** (소비자 AI 채팅 = 이것)
7. **전자차트**: 방문고객 인적사항 + 시술/수술 + 결제 항목

**[CRM 앱 화면]** 병원 정보 확인·수정 / 환자 예약 확인 / 재고 확인 / 음성 입력(시술 중 환자 상태 코멘트, 사용제품, 담당자 → OPD)

**[소비자 앱]**
1. 챗봇: 시술 고민 상담 / 병원과 실시간 영상·음성 상담 + 라이브(**Coming soon**) / 시술 설명 + 가격·프로모션 → 병원 추천(이름/의사/위치/영업일) / 예약 도우미 → 결제
2. 내 사진 + 희망 개선점 + 관심시술 → B&A 학습 기반 제안 (**Coming soon**)
3. 사주/관상 → 시술 제안 (**Coming soon**)
4. 위치기반 주변 100m 프로모션 알림 (**Coming soon**)

---

## 5. AI 채팅 시나리오 (확정 초안 — EN/TH로 번역해서 사용)

**첫 질문 A: "얼굴 톤이 하얘지고 밝아지고 싶어요"**
- 답변: 레이저 토닝 / 스킨부스터(리쥬란·엑소좀) / 물광주사 비교 설명
- 예상1: "레이저 토닝이랑 주사 중 뭐가 더 효과 좋아요?"
- 예상2: "몇 번 받아야 효과가 보여요? 가격은요?"
- 유도: "화이트닝 시술하는 클리닉을 추천받아 볼까요?"
- 2회차 예상: "다운타임 있어요?" / "민감한 피부도 괜찮나요?"
- 2회차 유도: "지금 화이트닝 프로모션 중인 클리닉 보여드릴까요?"

**첫 질문 B: "턱선이 갸름해지고 싶어요 (V라인)"**
- 답변: 사각턱 보톡스(Botox·Nabota) / RF 리프팅(Oligio) / 필러
- 예상1: "보톡스랑 리프팅은 뭐가 달라요?"
- 예상2: "효과가 얼마나 오래 가요?"
- 유도: "V라인 시술 잘하는 클리닉을 찾아볼까요?"
- 2회차 유도: "V라인 프로모션 중인 클리닉 보여드릴까요?"
- (B는 재고의 보톡스·Oligio와 연결되어 "시술 → 재고 차감" 시연에 유리)

규칙: 모든 예상질문·유도질문은 클릭 연동. 유도질문은 **1회차부터** 예약 플로우로 이동.

---

## 6. 가라 데이터 규모 (seed JSON)

| 항목 | 수량 |
|---|---|
| 클리닉 | **10곳** — 그중 **3곳은 마스터 계정 + 지점 3개**, 나머지 7곳은 단일(마스터만) |
| 프로모션 | 클리닉당 **3개** |
| 시술 리스트 | 클리닉당 **9개** |
| 영업시간 | 클리닉별 |
| 고객 | **지점 단위 5명** (단일 클리닉은 클리닉당 5명) |
| 재고 | **지점 단위 4개**, 22개 제품 풀에서 랜덤, 수량 **10~3000 랜덤**, 일부는 50 이하가 되게 |
| 의사/직원 | 클리닉·지점별 (담당자·사원번호 입력용) |
| 인박스 문의 | LINE · Meta · 앱 채팅 가라 대화 |
| 기타 | 후기·후기코드·커미션, 공지·팝업, 이전 AI 대화 리스트, 이전 CRM 입력 기록, SMS 로그 |

고객정보·클리닉정보 필드는 필요한 항목을 판단해 구성 (예: 고객 — 이름, 연락처, 생년월일, 국적, 유입경로, 관심시술, 방문이력, 담당의, 결제내역 / 클리닉 — 이름, 지점, 주소, 지도, 주차, 영업시간, 의사, 시술·가격, 프로모션, 후기, 평점)

---

## 7. 이미지 에셋

### 완료: 제품 이미지 22개 (`heybeauty-products.zip`, 800×800 흰 배경 PNG)
사용자 제공 스크린샷에서 크롭

| ID | 파일명 | 분류 |
|---|---|---|
| P01 | P01_flore-max.png | Filler |
| P02 | P02_belotero-intense.png | Filler |
| P03 | P03_restylane-lidocaine.png | Filler |
| P04 | P04_relife-definisse.png | Filler |
| P05 | P05_restylane-skinboosters-vital.png | Skin booster |
| P06 | P06_restylane-lyft.png | Filler |
| P07 | P07_juvederm-volite.png | Skin booster |
| P08 | P08_juvederm-voluma.png | Filler |
| P09 | P09_juvederm-ultra-plus-xc.png | Filler |
| P10 | P10_botox-cosmetic-100u.png | Toxin |
| P11 | P11_xeomin-100u.png | Toxin |
| P12 | P12_botulax-100u.png | Toxin |
| P13 | P13_dysport-500u.png | Toxin |
| P14 | P14_nabota-100u.png | Toxin |
| P15 | P15_aestox-100u.png | Toxin |
| P16 | P16_rejuran.png | Skin booster |
| P17 | P17_ncf-140h.png | Meso |
| P18 | P18_radiesse.png | Biostimulator |
| P19 | P19_asce-plus-exosome.png | Skin booster |
| P20 | P20_made-collagen.png | Meso |
| P21 | P21_neauvia-hydrodeluxe.png | Biostimulator |
| P22 | P22_oligio-rf-device.png | Device |

(필러 사진은 원본에서 박스가 겹쳐 있어 모서리에 옆 박스가 조금 보임)

### 할 일: 직접 제작
- **ID카드 예시** (가상 인물·번호 + DEMO 워터마크)
- **송금 슬립 예시** (가상 은행 "HeyBank" 등 + DEMO 워터마크)
- 제품 박스 라벨 예시(OCR 시연용: 제품명·용량·Lot·유통기한)
- 의사/클리닉 이미지는 로고 없는 플레이스홀더

### 사용자에게 추가 요청한 이미지 (아직 미수령)
1. 레이저 기기 2~3개 (피코/토닝, 울쎄라·써마지 등 리프팅)
2. 화이트닝 주사/IV 제품 1~2개 (글루타치온, 비타민C 등)
3. 소모품 2~3개 (주사기, 마취크림, 거즈 등)

---

## 8. 아키텍처 (초안)

- 초안 링크(claude.ai 아티팩트, Mermaid): https://claude.ai/artifact/VHMa5DFTMv3ceVobDXKUqn
- 구성: 사용자(소비자 / 파트너 마스터·지점 / RAON 어드민) → 웹(탭 4개) → Mock 서비스(LINE 로그인, PromptPay QR, 슬립·ID·제품박스 OCR, 3초 음성인식, LINE·Meta 인박스, SMS, AI 답변 시나리오) → 데이터(클리닉·지점·시술·프로모션·의사·영업시간 / 유저·고객·예약·결제·후기·후기코드·커미션 / AI채팅·클리닉채팅·메시지·입력기록 / 제품·재고·입출고로그·경고기준 / 공지·팝업·차단·SMS로그) + Realtime + Storage(이미지)
- 핵심 흐름: AI 질문 → 예약유도 → 예약·QR → 슬립 첨부 → 송금확인 → 클리닉 채팅 → CRM 인박스 → 방문 ID카드 OCR → 시술 후 음성입력 → 재고 차감 → 어드민 반영 → 경고 → 구매하기
- **archify로 정식 다이어그램 다시 그리기**

---

## 9. 다음 할 일

### 완료
- [x] **스택·배포 순서 확정** (2026-09-17): Next.js 15 + storage 어댑터 localStorage로 **Phase 1 로컬 시연** → **Phase 2 Supabase + GitHub + Vercel**
- [x] **기본 언어 확정** (2026-09-17): **개발·검수는 전부 한국어로 먼저 진행** → 검수 완료 후 태국어로 전환
  - 홈 탭: 언어 버튼 UI는 그대로 두고 **기본값 KO**, EN·TH 번역은 검수 후 채움 (ZH·RU 항목명 / AR Coming soon 은 §2-7 유지)
  - 어드민·파트너 CRM·회사소개: 언어 버튼 없이 한국어 고정
- [x] **디자인 방향 확정** (2026-09-17): 핑크 글래스모피즘 → §10 참조. 세부 디자인은 나중에 재검토
- [x] seed 데이터 작성 (§6) → `lib/seed.ts` (`SEED_VERSION` 으로 스키마 변경 시 자동 초기화)
- [x] 화면 제작: 홈 / 어드민 / 파트너 CRM / 회사소개 4개 탭 전부
- [x] 연동 테스트: 재고 차감 ↔ 어드민 숫자, 빨간 경고 양쪽 표시, 수정 저장 유지
- [x] 데모 리셋 버튼 (어드민 탭 하단)
- [x] **1차 사용자 피드백 22건 반영 + 콘솔 이슈 13건 → 0건** (2026-09-17) → §11

### 남은 일 (우선순위 순)

**A. 다음 세션에서 바로 할 것**
- [ ] **Vercel 배포 (가짜 AI 상태 그대로, 비용 0원)** — 폰에서 카메라·음성 실제 동작 확인이 목적 (HTTPS 필요)
  - [ ] GitHub 저장소 생성 + 첫 커밋 (현재 git 저장소 아님)
  - [ ] Vercel 연결 → 배포
  - [ ] 데모 URL 비밀번호 걸기 (아무나 못 들어오게)
- [ ] **Claude API 연동** → §12 참조 (사용자 결정 완료: 2026-09-17)

**B. 사용자가 직접 채울 것**
- [ ] 후기 사진 (어드민 → 후기 · 커미션, 사진 칸 준비됨)
- [ ] 팝업 이미지 (어드민 → 공지 · 팝업, 사진 칸 준비됨)
- [ ] CRM 카메라 DEMO 샘플 이미지 교체
- [ ] 클리닉 사진 (`public/clinics/` 폴더 비어 있음 → 지금은 그라데이션 플레이스홀더)

**C. 나중에**
- [ ] ID카드·슬립·제품박스 예시 이미지 제작 (§7) — 가상 정보 + DEMO 워터마크
- [ ] EN/TH 번역 + ZH/RU 항목명 사전 — 한국어 검수 완료 후
  - 현재 `lib/i18n.ts` 사전은 홈 탭 주요 라벨만. ChatView·BookingFlow·어드민·파트너·`lib/scenario.ts` 문구는 한국어 고정
- [ ] 디자인 재검토 (Pinterest 레퍼런스 대비 색감·굴절·선 굵기)
- [ ] **archify 아키텍처 다이어그램** — Vercel 배포 직전, 사용자 승인 후
- [ ] Phase 2: Supabase 테이블 + RLS + Realtime (localStorage → Supabase 교체는 `lib/storage/` 어댑터만 갈아끼우면 됨)

---

## 10. 디자인 가이드 (핑크 글래스모피즘)

레퍼런스: `assets/design-refs/pin_main.jpg` (출처 Pinterest, 사용자 제공)
방향: **유리 굴절(glassmorphism) 카드 + 핑크 그라데이션 배경 + 아주 얇은 선 + 큰 라운드**. 세부는 추후 업데이트.

### 색상
| 토큰 | 값 | 용도 |
|---|---|---|
| `--pink-50` | `#FDF2F5` | 페이지 베이스 |
| `--pink-200` | `#F5B8C4` | 그라데이션 상단 |
| `--pink-400` | `#F09AAE` | 포인트(구체·CTA 강조) |
| `--pink-600` | `#D97C93` | 그라데이션 하단·호버 |
| `--ink` | `#1C1C1C` | 블랙 카드·주요 버튼·본문 |
| `--ink-sub` | `#8A8A8A` | 보조 텍스트·비활성 |
| `--glass-bg` | `rgba(255,255,255,0.55)` | 유리 카드 배경 |
| `--glass-border` | `rgba(255,255,255,0.60)` | 유리 카드 테두리 |
| `--surface` | `#F6F6F6` / `#FFFFFF` | 불투명 카드(CRM·어드민) |
| `--danger` | `#E23D3D` | 재고 경고 빨간색 |

### 굴절 / 테두리 / 라운드
- 유리 효과: `backdrop-filter: blur(24px) saturate(140%)` + `--glass-bg` + inner highlight `inset 0 1px 0 rgba(255,255,255,0.7)`
- **선 굵기는 전부 1px** (얇게). 강조가 필요해도 1.5px 초과 금지
- 라운드: 카드 `28~32px` / 입력창·버튼 **pill `999px`** / 작은 뱃지 `999px` / CRM 테이블 셀 `12px`
- 그림자: `0 8px 32px rgba(0,0,0,0.08)` (떠 있는 느낌, 진하지 않게)

### 타이포
- 폰트: **Pretendard** (한글·영문 동시 대응). 숫자 강조 구간은 굵기 대비 크게 (레퍼런스의 `Jan / 2025`처럼 Bold + Light 회색 조합)
- 본문 15~16px / 라벨 13px / 섹션 타이틀 28~40px

### 컴포넌트 규칙
- 입력 필드: pill 형태 + 왼쪽에 **원형 아이콘 배지**(얇은 선 아이콘)
- 주요 CTA: **블랙 pill 버튼 + 오른쪽 흰 원형 화살표**
- 보조 버튼: 흰 pill + 1px 테두리
- 섹션 대비용 **블랙 카드** 1개씩 배치 가능 (레퍼런스의 References 카드)
- CRM·어드민은 유리 효과를 약하게(불투명 화이트 위주) — 정보 가독성 우선

### 애니메이션 (넣을 것)
| 위치 | 효과 |
|---|---|
| 페이지·카드 진입 | fade + 12px 위로 slide, stagger 60ms |
| 카드 hover | 4px 떠오름 + 그림자 확대 (200ms ease-out) |
| 탭 전환 | 밑줄 인디케이터 slide + 콘텐츠 crossfade |
| AI 채팅 | 말풍선 pop-in + 점 3개 typing 인디케이터 |
| 마이크 입력 | 3초 듣기 — 원형 pulse ring 확산 + 파형 bar |
| 카메라 OCR | 스캔 라인 훑기 → 텍스트 한 줄씩 나타남 |
| PromptPay QR | scale-in + 유리 카드 shimmer 1회 |
| 송금 확인 | 체크마크 stroke draw |
| 재고 숫자 변경 | 카운트 업/다운 롤링, 경고 진입 시 빨강 pulse 2회 |
| 토스트("저장되었습니다") | 아래에서 slide-up |
- 전부 CSS transition / keyframes 기반, 무거운 라이브러리 없이
- `prefers-reduced-motion` 존중

---

## 11. 작업 로그

### 2026-09-17 — 1차 피드백 반영 (22건) + 이슈 13건 해결

검증: `npx tsc --noEmit` 에러 0 / `npm run build` 통과 / 브라우저 콘솔 경고·에러 0 / 모바일 375px 가로 넘침 0

**홈 탭**
| 요청 | 처리 |
|---|---|
| 대화 목록 지우기 | 사이드바에서 제거. **콘솔 이슈 13건의 원인**이 여기였음(대화 제목이 겹쳐 React key 중복) → 0건 |
| 검정색 → `#353839` | `--ink` 토큰 교체 (§10 표의 `#1C1C1C` 대체) |
| 클리닉 사진 | 카드에 이미지 슬롯 추가. `public/clinics/` 비어 있어 지금은 그라데이션 플레이스홀더 |
| 카테고리별 보기 | 카테고리 클릭 시 필터 + 개수 표시 |
| 전체 언어 작용 (AR 제외) | `lib/i18n.ts` 연결. AR은 `comingSoon: true`로 KO 폴백 |

**어드민 탭**
| 요청 | 처리 |
|---|---|
| 전체 재고 `구매하기` 불필요 | **`상세 내용보기`**로 교체. 누르면 16행 스펙 + 입출고 기록. 기존 "+100 자동증가"는 삭제 |
| 후기 사진 칸 | `components/ui/PhotoPicker.tsx` 신규 — 후기마다 사진 첨부 |
| 유저 관리 | `공지 보내기`(해당 유저 지정 발송) + `총 구매금액`·`이용 건수` (예약 × 시술가로 계산) |
| 클리닉 정보 | 유저 관리처럼 **리스트 먼저** → 클릭 시 수정. `클리닉 등록하기` 신규 (일반/지점 구분, 지점 수 입력 시 지점·계정 자동 생성) |
| 팝업 이미지 | PhotoPicker 연결 |
| 계정 · 비번 | `클리닉` / `유저` 탭 분리 + **사용가능 / 홀드 / 차단** 3단계. 유저 계정 차단은 유저 차단 상태와 자동 연동 |

**파트너 CRM 탭**
| 요청 | 처리 |
|---|---|
| 이전 입력기록을 텍스트 칸 안으로 | 확인 화면 textarea 아래 접이식으로 이동 |
| 사진 찍기 / 말하기 버튼 크게 | 큰 카드 2개 → 누르면 다음 페이지로 이동 |
| 실제 카메라 연동 | **진짜 동작**. `getUserMedia` 사용, 별도 설치 없음 |
| 음성 | **진짜 동작**. Web Speech API 한국어 받아쓰기, 미지원 시 데모 폴백 |
| 사진 → 텍스트(OCR) | **아직 DEMO 샘플** → §12에서 Claude API로 해결 |
| 통합 인박스 App → 헤이뷰티 | 화면 표시만 변경. 저장 데이터는 `"App"` 유지 (마이그레이션 불필요) |
| 플랫폼 아이콘 | LINE 초록 `L` / Meta 파랑 `f` / 헤이뷰티 핑크 `H` |
| 고객 관리 | 클릭 → 상세(누적 결제·방문 기록·메모 수정). **국가 / 성별 / 유입경로** 필터 |
| 예약 확인 `수정` 버튼 | 취소 옆에 추가 → 날짜·시간·담당의 수정 |
| 전자차트 | 클릭 → 상세 + `프린트`(실제 동작) + `PDF 다운로드`(토스트만, 요청대로 기능 없음) |
| 재고 구매 페이지 | `구매하기` → 제품 선택·단가·수량·결제예정액 → **"주문이 완료되었습니다 / 이메일을 확인해주세요"** + 주문번호. 지점에 없는 제품 교차 추천 포함 |
| 재고 하단 광고 | `바로 구매하러 가기` 카드 → 같은 구매 페이지로 |

**타입 추가** (`lib/types.ts`): `Product.unitPriceTHB`·`unit` / `Customer.gender`·`nationality` / `Review.images` / `Popup.image` / `Notice.target` / `Account.kind`·`status`

**주의:** 개발 서버가 떠 있는 상태에서 `npm run build`를 돌리면 `.next` 폴더가 덮어써져 화면이 `Internal Server Error`가 됨. 서버 재시작하면 정상 (코드 문제 아님).

---

## 12. Claude API 연동 계획 (다음 세션)

**결정 (2026-09-17):** 붙이기로 확정. 새 채팅에서 진행.

### 핵심 개념
- 지금 앱은 **화면(브라우저)**만 있음 → Next.js **Route Handler(`app/api/`)**로 서버를 만들고, 그 서버만 Claude를 호출
- Vercel이 이 서버를 자동으로 돌려줌

### 반드시 지킬 것 (보안)
1. **API 키는 서버 코드에서만 사용.** 화면 코드(`"use client"`)에 절대 넣지 않음 — 브라우저에서 다 보임
2. 키는 Vercel `Environment Variables`에 저장. `NEXT_PUBLIC_` 접두사 **금지** (그거 붙이면 브라우저에 노출됨)
3. `.env.local`은 `.gitignore`에 넣어 GitHub에 올리지 않음
4. 배포 URL에 비밀번호를 걸어 아무나 API를 쓰지 못하게 (요금 방어)
5. console.anthropic.com에서 **Spend limit** 먼저 설정

### 과금 (사용자 질문 답변)
- **Claude Code 구독과 API는 완전히 별개.** 토큰 공유 안 됨
- Claude Code 구독 = 개발용(도도 본인) / API = 배포된 앱이 손님에게 서비스할 때
- 즉 **구독료 + API 요금** 2개. 단 API는 안 쓰면 0원
- 정확한 단가는 console.anthropic.com 공식 표 확인

### 적용 범위 (합의된 방향)
| 기능 | 방침 | 이유 |
|---|---|---|
| CRM 사진 → 텍스트(OCR) | **진짜 Claude** | 데모의 "우와" 포인트. 약품 박스 Lot·유통기한은 무료 OCR(Tesseract)로는 자주 틀림 |
| CRM 음성 → 차트 요약 | 진짜 Claude (선택) | 받아쓰기는 이미 브라우저로 무료 해결됨. 정리만 AI |
| 홈 AI 채팅 | **가짜 대본 유지** (`lib/scenario.ts`) | 투자자 시연 중 AI가 느려지거나 엉뚱한 답을 하면 리스크. 안정성 우선 |

### 작업 순서
1. [x] `npm i @anthropic-ai/sdk` (0.126.0)
2. [x] `app/api/ocr/route.ts` 생성 — 이미지 dataURL → `claude-opus-4-7` vision → `{제품명, 용량, Lot번호, 유통기한, 유통형태}` JSON
3. [x] `components/partner/AiInput.tsx` `useShot()`에서 `/api/ocr` 호출, 판독 중 버튼은 "AI가 읽는 중..."으로 잠김
4. [x] 실패 시 빈 양식 + "직접 입력해주세요" 토스트로 폴백 (키 없음·네트워크 오류·판독 불가 모두 동일)
5. [ ] 로컬 테스트 → Vercel 환경변수 등록 → 배포 확인 — **API 키 발급 대기 중**

### 남은 것 (2026-09-18)
`.env.local`에 `ANTHROPIC_API_KEY=sk-ant-...` 한 줄을 넣고 dev 서버를 재시작하면 진짜 판독이 켜짐.
키가 없으면 서버가 `503 no_api_key`를 돌려주고 화면은 폴백으로 넘어가므로 시연은 그대로 돌아감.
