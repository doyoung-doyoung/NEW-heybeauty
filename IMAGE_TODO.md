# 만들어야 할 이미지 목록

Hey! Beauty 데모에 넣을 사진 정리. 만든 뒤 아래 "저장 위치"에 넣으면 코드에 연결한다.
(2026-09-20 코드 대조 완료)

## 한눈에 보기

| 무엇 | 장수 | 크기 | 저장 위치 | 지금 상태 |
|---|---|---|---|---|
| 후기 사진 | 4장 | 800 × 800 (정사각형) | `public/reviews/` | **완료** (2026-09-20) |
| 팝업 이미지 | 2장 | 1200 × 500 (가로로 긴) | `public/popups/` | **완료** (2026-09-20) |
| 클리닉 사진 | 10장 중 **7장** | 1200 × 675 (가로 16:9) | `public/clinics/` | C01~C07 **완료** (2026-09-20) · C08~C10은 자동 커버 유지 |
| 제품 박스 | 22장 | — | `public/products/` | **완료** |

> 남은 건 클리닉 **C08 · C09 · C10 세 장**뿐 (크레딧 부족으로 7장만 생성).
> 사진이 없어도 그라데이션 커버가 자동으로 떠서 화면이 비지 않으니 급하지 않다.

**이미지 만드는 법:** 클로드 코드에서 바로 생성한다. `gpt_image_2_5` 모델,
`quality: medium` / `resolution: 1k` 기준 **1장당 1크레딧**. (`high`로 올리면 2크레딧)
생성 후 `sips`로 규격에 맞게 변환해 `public/` 밑에 넣는다.

---

## 1. 후기 사진 (4장) — 완료

후기는 **2건**. 각각 2장씩 붙인다. 후기 내용에 맞는 사진이어야 자연스럽다.

| 파일명 | 붙일 후기 | 후기 내용 |
|---|---|---|
| `RV1-1.jpg`, `RV1-2.jpg` | RV1 · 사얌 글로우 클리닉 · 별 5개 | "레이저 토닝 3회 받았는데 톤이 확실히 밝아졌어요. 상담도 친절했습니다." |
| `RV2-1.jpg`, `RV2-2.jpg` | RV2 · 방콕 루미에르 클리닉 · 별 4개 | "보톡스 맞고 라인이 정리된 느낌이에요. 대기 시간이 조금 길었어요." |

**찍을 내용:** 손님이 폰으로 직접 찍은 느낌. 시술실 한켠, 받은 제품, 클리닉 입구 등.
너무 광고 같지 않게, 살짝 평범한 게 오히려 진짜 같다.

**주의:** 화면에서 **가로 3칸 그리드 안에 높이 80px**로 아주 작게 나온다.
글씨가 들어가면 안 보이니 넣지 말 것. 피사체 하나만 크게.

---

## 2. 팝업 이미지 (2장) — 완료

| 파일명 | 내용 | 비고 |
|---|---|---|
| `PP2.jpg` | 레이저 시술 장면 (클리닉이 핸드피스로 피부 시술) | **기본값** |
| `PP1.jpg` | 화장품 제품 배너 | 예비 |

어드민 → **공지 · 팝업 → 팝업** 탭에서 "기본 이미지에서 고르기"로 둘 중 하나를 눌러 고른다.
직접 올리기도 그대로 쓸 수 있다.

**새 팝업 이미지를 추가하려면** 파일을 `public/popups/`에 넣고
`components/tabs/AdminTab.tsx`의 `POPUP_PRESETS` 목록에 한 줄 추가하면 된다.

**찍을 내용:** 프로모션 배너 느낌. 가로로 긴 형태. 화장품보다 **시술 장면**이 잘 맞는다.
제목·본문 글씨는 사진 아래에 따로 나오니 **사진에는 글씨를 넣지 않는 게 깔끔하다.**

**주의:** 높이 160px로 위아래가 잘린다. 중요한 건 한가운데.

---

## 3. 클리닉 사진 (10장 중 7장 완료)

**파일명은 반드시 아래 그대로, 확장자도 반드시 `.jpg`.** 코드가 이 이름으로 찾는다.

| 파일명 | 클리닉 이름 | 동네 | 지점 | 상태 · 만든 콘셉트 |
|---|---|---|---|---|
| `C01.jpg` | 사얌 글로우 클리닉 | 사얌 | 3개 지점 | 완료 · 화이트+로즈골드 곡선 리셉션 |
| `C02.jpg` | 방콕 루미에르 클리닉 | 프롬퐁 | 3개 지점 | 완료 · 크림 대리석+황동, 따뜻한 조명 |
| `C03.jpg` | 아속 스킨랩 | 아속 | 3개 지점 | 완료 · 올화이트 클리니컬 랩, 피부분석기 |
| `C04.jpg` | 통러 뷰티하우스 | 통러 | 단일 | 완료 · 원목+유리 외관, 열대 식물 |
| `C05.jpg` | 프롬퐁 더마클리닉 | 프롬퐁 | 단일 | 완료 · 밝은 상담실, 오크 책상 |
| `C06.jpg` | 실롬 라디언스 | 실롬 | 단일 | 완료 · 고층 라운지, 통유리 시티뷰 |
| `C07.jpg` | 아리 스킨스튜디오 | 아리 | 단일 | 완료 · 아치 도어, 우드+블러시핑크 |
| `C08.jpg` | 에까마이 오라 클리닉 | 에까마이 | 단일 | **남음** |
| `C09.jpg` | 라차다 퓨어덤 | 라차다 | 단일 | **남음** |
| `C10.jpg` | 차이나타운 벨르 | 차이나타운 | 단일 | **남음** |

**C08~C10을 만들 때:** 위 7장과 분위기가 겹치지 않게 하면 좋다.
에까마이는 모던 로프트, 라차다는 넓은 대기실, 차이나타운은 샵하우스 개조 느낌 정도가 안 겹친다.

**만드는 순서 (7장 만들 때 쓴 방법):**

1. `generate_image_batch` · 모델 `gpt_image_2_5` · `quality: medium` · `resolution: 1k` · `aspect_ratio: 16:9`
2. 프롬프트 끝에 반드시 넣을 것: 글씨·간판·로고 금지, 사람 얼굴 금지,
   **"위아래가 잘리니 핵심 피사체를 상하좌우 한가운데"**
3. 결과는 1344×752로 나온다. 규격에 맞추는 명령:
   ```bash
   sips -s format jpeg -s formatOptions 82 -z 675 1207 입력.png --out C01.jpg
   sips -c 675 1200 C01.jpg      # 가운데 기준으로 1200×675로 자름
   ```

**함정 — zsh 배열은 1부터 시작한다.** URL 목록을 배열에 담고 `${U[$((i-1))]}`처럼
0부터 세면 **첫 장이 비고 나머지가 한 칸씩 밀려** 클리닉과 사진이 어긋난다.
받은 뒤 `shasum public/clinics/*.jpg | awk '{print $1}' | sort -u | wc -l`로
**장수만큼 서로 다른지 반드시 확인할 것.**

**찍을 내용:** 방콕의 고급 뷰티 클리닉 외관 또는 로비·상담실 내부.
밝고 깨끗한 느낌. 사람 얼굴은 안 나오는 게 안전 (초상권).

**주의 — 위아래가 잘린다.** 목록 카드에서 높이 144px, 상세 화면에서 192px(큰 화면 240px)로
납작하게 잘려 나온다. 간판·인테리어 포인트는 **한가운데**에 오도록 만들 것.

**한 장만 넣어도 된다.** 10장 중 일부만 넣으면 넣은 것만 사진으로 바뀌고
나머지는 자동 커버가 유지된다. 섞여 보여도 어색하지 않다.

---

## 만든 다음에 할 일

클리닉 사진은 `lib/seed.ts`에 `/clinics/C01.jpg` 식으로 **이미 연결돼 있다.**
`public/clinics/` 폴더를 만들고 파일만 넣으면 끝. 코드 수정 불필요.

그 다음 GitHub에 올리면 1분 뒤 배포 사이트에 자동 반영된다.

### 주의 — 데이터 버전 (`SEED_VERSION`)

데모 데이터는 브라우저에 저장돼 있고, `lib/seed.ts`의 `SEED_VERSION` 숫자가 바뀔 때만
새로 불러온다. **`lib/seed.ts`의 데이터 내용을 고쳤다면 이 숫자도 같이 올려야 한다.**
안 올리면 이미 사이트를 열어본 사람에게는 옛 데이터가 그대로 남아 바뀐 게 안 보인다.
(후기·팝업 경로 연결로 2 → 3, 팝업 기본 이미지를 시술 사진으로 바꾸며 3 → 4로 올림.
클리닉 사진은 코드를 안 건드리니 더 올릴 필요 없음.)

## 파일 형식 참고

- **사진은 `.jpg`**, 로고처럼 배경이 투명해야 하면 `.png`
- 한 장에 **500KB 이하** 권장 (너무 크면 폰에서 느리게 뜸)
- 파일명에 한글·띄어쓰기 금지
